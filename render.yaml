services:
  - type: web
    name: logistics-app
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node legacy-app/app.js
    envVars:
      - key: NODE_ENV
        value: production
